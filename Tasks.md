Tasks

Done 1.) Create optional definitions that chooses actors on the map that would like to roll all of their dice themselves. If actors are in this list, the model should pause, give a pop-up explaining what they are rolling (i.e. Dex save, to hit, etc...). 
Done 2.) Wall creation. I would like for there to be an option for the user to create walls. This should work by having an option in the GUI to select hexes on the map that are walls that actors cannot pass through. The walls should be highlighted on the map and the engine should consider these walls as direct blocks that the actors cannot pass through. When deciding when and where to move, the engine should consider rerouting around these paths.
Done 3.) GUI encounter window positions editable. I would like to be able to allow the user to move around grouped sections on the GUI. Some that come to mind are the initiative order window, the action selections, output text window, the map itself. Look through to see what else could potentially be added to this as well. The groups should be able to both be moved around inside the window, resized, and minimized.
Done 4.) Some spells persist on the map and create obstacles that can do various things to actors that go through them (i.e. do damage, slow enemies, etc..). Can you work through the best way to both allow the users to use these spells in the GUI and implement was to show the colors. Also work in the engine itself. Find the best way to have these persist and in the decision logic, have actors account for the obstacle and calculate if its best to go through or around these objects. This should also consider that these spells do not last forever. They have a given time limit and can be ended early if the caster either takes damage and fails a concentration saving throw or they cast another concentration spell.
Done 5.) Allow for monster classes to also have their choices decided by the user like players currently are. This should be saved in a list like manual roller and should allow for both players and monsters to be optional controlled by the user or the engine. By default players should have manual actions and monsters automated.
Done 6.) Add debugger mode. Turn off all print statements that are printing things out doing a model run. Create an optional flag in main.py to turn on print statements that could help coders deduce where the problem in crash occured.
Done 7.) Make the right click pop up window that is displayed when right clicking an actor to look cleaner. It should have a health bar displayed.
Done 8.) Move "spell Area" button from the map tool bar to the action toolbar. Rename it to be more consistant with what it actually does, its really the button to select target of any action. Also, I am getting crashes when this selects an invalid target (actor targets self or empty hex). Can you add a check for this and make this selections invalid in the gui. Also, when selecting a new spell this flips off/on wrong. Can you also make it so when selecting a new action, the spell area toggle gets set to on?
Done 9.) implement the distance calc button to work appropriately. It should allow users to click on the starting hex, click on another hex and calc the distance between them. This should consider things like walls and calculate the distance given the possible path.
Done 10.) In the right click pop-up window, have an optional "show previous turn" button that will show the path it moved (needs to handle not moving at all) and highlight its actions in the log. When done, these path and log highlighting needs to go away.
Done 11.) Outside of the encounter window, create a spell creation tab that will generate a new spell. This should add the spell to the spells.json file and request all of the information needed for the new spell to work inside of the model.
Done 12.) Add a default image to the map in the encounter builder if one is not selected.
Skip 13.) In the encounter builder add a build that autogenerates an encounter for the given party bassed off of selected difficulty. This should use the non-interactive encounter simulation and score how well the party is expected to do.
Done 14.) Fix actor hex icon shifting and not being centered over time. Also, the red outline indicating who's turn it is on the map will some times not disappear correctly. I believe that both issues is caused by the same problem. Can you investigate and find a solution.
Done 15.) Have a button to pop up secondary map display that the user could show players on secondary monitor. This display should only mimic the current map but should not be able to be interacted with. Also add a "Fog of war" feature on the main map display that will cover up selected hexes on the player display. This should act like the wall button with a tooltip pop-up for create and delete fog. On the main display, the fog should be a very dim grey and should not prevent the user from interacting with actors on the map.
Done 16.) Make it so manual roll and manual action lists you can click on the actor name to check them as well. Also add a "select all" button above the players and monsters list for both manual roll and action
Done 17.) Dash action has the actor not stop at the current hex that they are on. I believe its because its calling nearest free hex and seeing itself on the hex that it is on.
18.) undo turn needs to work with persistent spells. Removing the spell if it was cast on the previous turn
19.) in right click window add optional button to set stats (to handle dm adjudication outside of the engine). Needs to handle setting actors health to 0 (should delete the actor from GUI and engine on end turn). This should also work for setting conditions such as inconpacitated.
20.) Link the log with the undo turn action feature. I would like the log to know what turn its connected to. When the user right clicks on the log, have a pop-up that says "set to current turn". When thats click, a warning should ask if the user understands this as I want it to delete everything after that turn in the log and reset the engine and GUI map to that turn.
21.) Add a new type from task 3 that is the party members current health. These should show the party members name, icon, heatlh bar, and health numbers. Optionally allow the user to right click on these docks and have the same show on player window. Like the map, these should not interactable, just mimic whats on the users screen.
22.) Add bonus actions to turn selector and engine. Example: Healing word is a combat spell that takes a bonus action to cast, but the current logic does not allow for this. The engine and GUI should allow for spells casted as a bonus action and know that you cannot cast another spell afterwards, but still have an action to attack or dash.
23.) Fix the character creation tab. Right now the character creation tab does not work appropriately. If you select a prebuilt character, it will crash. This new tab should include everything from the newchar.json as selections. Similarly to the spell creation tool, the tab should not allow for saving a character that will break the engine/GUI logic.
24.) For both the engine and GUI, have it understand the size of actors. Actors that are large should take up 3 hexes in a triangle, huge 7 hexes and so forth. Moving needs to account for both places the actor can stand (cant move to a hex that does not permit its sizes due to other actors) as well as not allowing other actors to move into the space that creature takes up. Range of actions needs to also account for the new size of a larger creature. Ranges from actor should take the range from the closest hex that that actor is taking up.
25.) If an action as already been performed this turn, grey out take turn and make it unclickable.
26.) Make sure take turn is applying the same features as move button. It should be reducing the actors current speed in the engine and redrawing the green movement circle to its new move speed for this turn.
27.) have the log be able to collapse at turn start. Meaning I would like to collapse from where it says "Current Turn: X" down to the next turn to minimize clutter and hide the actions taken that turn. This should be able to uncollapse as well.
28.) Make the Turn actions panel greyed out if you have yet to hit "Start Encounter". Likewise, make "Start Encounter" button greyed out after you have already started the encounter.
29.) Allow the actors locations to be edited if you have yet to start the encounter. This is for positioning of actors prior to combat, in case the user wants the actors arrange in a certain order. Upon hitting "Start Encounter" the actors should be moved into their current positions in the engine. This movement should not trigger in max speed violations, and should change the only the locations without triggering any effect.
30.) Right now, the Save Encounter feature does not work properly. This should allow for the encounter to be saved in its current state and be reopened as if no time has passed. The idea behind this is that if the group had to end their dnd session before finishing the fight, they need to be able to pick back up right where they left off. This should also work with the user arranging actors prior to starting combat (task 29).
31.) Show Previous turn button when you right click an actor should also work for monsters. Right now it only appears for players.
32.) When combat is done, either players group kill all enemies or all players are dead, show a pop-up that says "combat has ended!".
33.) The "map image" in the encounter builder should really have two layers. I want users to be able to select images on their drive, but once selected, it should save that image into a sub folder in the App. This is because I dont want to open the app to a bug where they move a folder with an image saved as a map and cause an encounter to bug out when it assumes its still in the older folder. Is this the best way to solve this issue? To have a "select from saved maps" button and a "import new map" button.
34.) In the encounter window, once the players simulate the combat, I need a way to rank the difficulty that the simulation expects it to. This should take into account how many spell slots the players used, their current health, if any player went down, and how many times they won. If there is a small chance for the players to lose, then it will be deadly. If the players lose every time, it should be Total Party Kill (TPK). And so forth, can you make an algorithm to rank these appropiately? Also display that ranking.

